# 지원사업 카드 목록 UI 개선 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/guide/[stage]`, `/programs`, 검색 결과 3곳의 지원사업 카드 목록을 기관별(정부/광역/기초) 섹션 + 2열 그리드로 바꾸고, 카드 패딩을 줄여 세 곳에 중복된 카드 마크업을 공통 컴포넌트로 통합한다.

**Architecture:** 기관별 그룹핑은 `lib/groupByAgency.ts`의 순수 함수로 분리하고, 카드 하나는 `components/programs/ProgramCard.tsx`, 섹션+그리드 레이아웃은 `components/programs/ProgramGroupedGrid.tsx`로 만든다. 기존 `Card` 컴포넌트에는 좁은 패딩용 `compact` prop만 추가한다. 세 페이지는 기존 카드 반복 렌더링 블록을 `<ProgramGroupedGrid programs={...} />` 한 줄로 교체한다.

**Tech Stack:** Next.js(App Router) · TypeScript · Tailwind CSS v4 · Vitest

## Global Constraints

- 상세 페이지(`/programs/[id]`)의 `Card` 사용(패딩 `p-6`)은 이번 변경으로 바뀌면 안 된다 — `Card`의 `compact` prop은 기본값 `false`로, 기존 호출부는 그대로 `p-6`을 써야 한다.
- 그룹 섹션은 정부 → 광역 → 기초 순서로, 프로그램이 있는 그룹만 표시한다(빈 그룹 섹션을 렌더링하지 않는다).
- 그리드는 좁은 화면에서 1열, `sm` 이상에서 2열이다(`grid-cols-1 sm:grid-cols-2`).
- 검색 결과(`ResultList`)에서만 "⚠️ 세부조건 확인 필요" 뱃지를 표시한다(`/guide/[stage]`, `/programs`에는 표시하지 않는다).
- `groupProgramsByAgency`는 순수 함수로 유닛 테스트한다. UI 컴포넌트(`ProgramCard`, `ProgramGroupedGrid`)는 기존 프로젝트 방침대로 자동화 테스트 없이 브라우저로 수동 검증한다.
- 모든 사용자 노출 텍스트는 한국어.

---

### Task 1: 기관별 그룹핑 순수 함수

**Files:**
- Create: `lib/groupByAgency.ts`
- Test: `lib/groupByAgency.test.ts`

**Interfaces:**
- Consumes: `AgencyType`, `SupportProgram` from `lib/schemas.ts`
- Produces: `AgencyGroup` (type: `{ agencyType: AgencyType; programs: SupportProgram[] }`), `groupProgramsByAgency(programs: SupportProgram[]): AgencyGroup[]` — Task 3(`ProgramGroupedGrid`)에서 사용

- [ ] **Step 1: 실패하는 테스트 작성**

`lib/groupByAgency.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { groupProgramsByAgency } from "./groupByAgency";
import type { SupportProgram } from "./schemas";

const base: SupportProgram = {
  id: "test-program",
  title: "테스트 지원사업",
  agencyType: "정부",
  agencyName: "보건복지부",
  region: "nationwide",
  stages: ["임신중"],
  category: "현금지원",
  summary: "테스트용",
  benefit: "테스트용",
  eligibility: {},
  applicationMethod: "온라인",
  applicationPeriod: "상시",
  officialLink: "https://www.gov.kr",
  lastVerifiedAt: "2026-08-06",
};

describe("groupProgramsByAgency", () => {
  it("returns an empty array for an empty input", () => {
    expect(groupProgramsByAgency([])).toEqual([]);
  });

  it("returns a single group when all programs share one agency type", () => {
    const local: SupportProgram = { ...base, id: "local", agencyType: "기초" };
    const local2: SupportProgram = { ...base, id: "local2", agencyType: "기초" };
    const result = groupProgramsByAgency([local, local2]);
    expect(result).toEqual([{ agencyType: "기초", programs: [local, local2] }]);
  });

  it("groups mixed agency types and orders them 정부 > 광역 > 기초, regardless of input order", () => {
    const local: SupportProgram = { ...base, id: "local", agencyType: "기초" };
    const metro: SupportProgram = { ...base, id: "metro", agencyType: "광역" };
    const gov: SupportProgram = { ...base, id: "gov", agencyType: "정부" };

    const result = groupProgramsByAgency([local, metro, gov]);

    expect(result.map((g) => g.agencyType)).toEqual(["정부", "광역", "기초"]);
    expect(result.map((g) => g.programs.map((p) => p.id))).toEqual([["gov"], ["metro"], ["local"]]);
  });

  it("preserves the relative order of programs within a group", () => {
    const govA: SupportProgram = { ...base, id: "gov-a", agencyType: "정부" };
    const govB: SupportProgram = { ...base, id: "gov-b", agencyType: "정부" };
    const result = groupProgramsByAgency([govA, govB]);
    expect(result[0].programs.map((p) => p.id)).toEqual(["gov-a", "gov-b"]);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run lib/groupByAgency.test.ts`
Expected: FAIL — `Cannot find module './groupByAgency'`

- [ ] **Step 3: `lib/groupByAgency.ts` 구현**

```ts
import type { AgencyType, SupportProgram } from "./schemas";

const AGENCY_ORDER: Record<AgencyType, number> = { 정부: 0, 광역: 1, 기초: 2 };

export interface AgencyGroup {
  agencyType: AgencyType;
  programs: SupportProgram[];
}

export function groupProgramsByAgency(programs: SupportProgram[]): AgencyGroup[] {
  const groups = new Map<AgencyType, SupportProgram[]>();
  for (const program of programs) {
    const existing = groups.get(program.agencyType);
    if (existing) {
      existing.push(program);
    } else {
      groups.set(program.agencyType, [program]);
    }
  }
  return Array.from(groups.entries())
    .map(([agencyType, groupPrograms]) => ({ agencyType, programs: groupPrograms }))
    .sort((a, b) => AGENCY_ORDER[a.agencyType] - AGENCY_ORDER[b.agencyType]);
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run lib/groupByAgency.test.ts`
Expected: PASS (4 tests)

- [ ] **Step 5: 커밋**

```bash
git add lib/groupByAgency.ts lib/groupByAgency.test.ts
git commit -m "feat: 기관별(정부/광역/기초) 지원사업 그룹핑 순수 함수"
```

---

### Task 2: Card compact prop 및 ProgramCard 컴포넌트

**Files:**
- Modify: `components/ui/Card.tsx`
- Create: `components/programs/ProgramCard.tsx`

**Interfaces:**
- Consumes: `SupportProgram` from `lib/schemas.ts`, `AgencyBadge` from `components/ui/Badge.tsx`, `Card` from `components/ui/Card.tsx`
- Produces: `Card`의 `compact?: boolean` prop(기본 `false`), `ProgramCard` 컴포넌트(`{ program: SupportProgram; showEligibilityWarning?: boolean }` props, 기본 `showEligibilityWarning = false`) — Task 3(`ProgramGroupedGrid`)에서 사용

- [ ] **Step 1: `components/ui/Card.tsx`에 `compact` prop 추가**

```tsx
import type { ReactNode } from "react";

export function Card({
  children,
  compact = false,
}: {
  children: ReactNode;
  compact?: boolean;
}) {
  return (
    <div className={`rounded-card border-2 border-brown/10 bg-white ${compact ? "p-4" : "p-6"}`}>
      {children}
    </div>
  );
}
```

- [ ] **Step 2: `components/programs/ProgramCard.tsx` 작성**

```tsx
import Link from "next/link";
import type { SupportProgram } from "@/lib/schemas";
import { AgencyBadge } from "@/components/ui/Badge";
import { Card } from "@/components/ui/Card";

// note가 이 사업의 유일한 자격조건 정보일 때만 확인 필요 뱃지를 띄운다.
// 나이/소득처럼 구조화된 필드로 이미 걸러진 경우 note는 부가 설명일 뿐이므로 뱃지를 띄우지 않는다.
function needsEligibilityCheck(program: SupportProgram): boolean {
  const { note, parentAgeMin, parentAgeMax, childAgeMonthsMin, childAgeMonthsMax, incomePercentMax } =
    program.eligibility;
  const hasStructuredCondition =
    parentAgeMin !== undefined ||
    parentAgeMax !== undefined ||
    childAgeMonthsMin !== undefined ||
    childAgeMonthsMax !== undefined ||
    incomePercentMax !== undefined;
  return Boolean(note) && !hasStructuredCondition;
}

export function ProgramCard({
  program,
  showEligibilityWarning = false,
}: {
  program: SupportProgram;
  showEligibilityWarning?: boolean;
}) {
  return (
    <Card compact>
      <div className="mb-1.5 flex flex-wrap items-center gap-2">
        <AgencyBadge agencyType={program.agencyType} agencyName={program.agencyName} />
        <span className="text-[13px] text-brown/60">{program.category}</span>
        {showEligibilityWarning && needsEligibilityCheck(program) && (
          <span className="inline-flex items-center rounded-full bg-soft-yellow px-2 py-0.5 text-[13px] font-bold text-brown">
            ⚠️ 세부조건 확인 필요
          </span>
        )}
      </div>
      <h3 className="font-display text-[19px] text-brown">{program.title}</h3>
      <p className="mt-1 text-brown/80">{program.summary}</p>
      <Link href={`/programs/${program.id}`} className="mt-3 inline-block font-bold text-coral">
        자세히 보기 →
      </Link>
    </Card>
  );
}
```

- [ ] **Step 3: 타입 체크로 검증**

Run: `npx tsc --noEmit`
Expected: 에러 없음 (아직 `ProgramCard`를 쓰는 곳이 없어 미사용 경고만 있을 수 있음 — 에러가 아니면 통과)

- [ ] **Step 4: 커밋**

```bash
git add components/ui/Card.tsx components/programs/ProgramCard.tsx
git commit -m "feat: Card compact 옵션 및 ProgramCard 컴포넌트"
```

---

### Task 3: ProgramGroupedGrid 컴포넌트

**Files:**
- Create: `components/programs/ProgramGroupedGrid.tsx`

**Interfaces:**
- Consumes: `groupProgramsByAgency` from `lib/groupByAgency.ts`, `SupportProgram` from `lib/schemas.ts`, `ProgramCard` from `components/programs/ProgramCard.tsx`
- Produces: `ProgramGroupedGrid` 컴포넌트(`{ programs: SupportProgram[]; showEligibilityWarning?: boolean }` props) — Task 4~6에서 각 페이지가 사용

- [ ] **Step 1: `components/programs/ProgramGroupedGrid.tsx` 작성**

```tsx
import { groupProgramsByAgency } from "@/lib/groupByAgency";
import type { AgencyType, SupportProgram } from "@/lib/schemas";
import { ProgramCard } from "./ProgramCard";

const AGENCY_SECTION_TITLE: Record<AgencyType, string> = {
  정부: "정부 지원사업",
  광역: "광역 지원사업",
  기초: "기초 지원사업",
};

export function ProgramGroupedGrid({
  programs,
  showEligibilityWarning = false,
}: {
  programs: SupportProgram[];
  showEligibilityWarning?: boolean;
}) {
  const groups = groupProgramsByAgency(programs);

  return (
    <div className="flex flex-col gap-8">
      {groups.map((group) => (
        <section key={group.agencyType}>
          <h2 className="font-display text-[19px] text-brown">
            {AGENCY_SECTION_TITLE[group.agencyType]}
          </h2>
          <div className="mt-4 grid grid-cols-1 gap-4 sm:grid-cols-2">
            {group.programs.map((program) => (
              <ProgramCard
                key={program.id}
                program={program}
                showEligibilityWarning={showEligibilityWarning}
              />
            ))}
          </div>
        </section>
      ))}
    </div>
  );
}
```

- [ ] **Step 2: 타입 체크로 검증**

Run: `npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 3: 커밋**

```bash
git add components/programs/ProgramGroupedGrid.tsx
git commit -m "feat: 기관별 섹션 + 2열 그리드 ProgramGroupedGrid 컴포넌트"
```

---

### Task 4: `/guide/[stage]` 페이지에 적용

**Files:**
- Modify: `app/guide/[stage]/page.tsx`

**Interfaces:**
- Consumes: `ProgramGroupedGrid` from `components/programs/ProgramGroupedGrid.tsx`

- [ ] **Step 1: `app/guide/[stage]/page.tsx` 전체를 아래로 교체**

```tsx
import { notFound } from "next/navigation";
import { stageSchema, type Stage } from "@/lib/schemas";
import { programs } from "@/lib/data/programs";
import { ProgramGroupedGrid } from "@/components/programs/ProgramGroupedGrid";

const STAGE_INTRO: Record<Stage, string> = {
  임신준비: "정부는 난임부부의 시술비 부담을 덜어주는 제도를 운영하고 있어요.",
  임신중: "정부와 서울시는 임신 중 진료비, 교통비, 고위험 임신 의료비 등을 지원해요.",
  출생출산: "출생 시 정부와 지자체에서 바우처, 현금지원, 산후조리 서비스를 제공해요.",
  영유아기: "0~1세 자녀를 위한 부모급여, 아동수당 등 현금성 지원이 이어져요.",
  유아: "2세 이상 자녀를 둔 가정도 아동수당 등 계속 지원을 받을 수 있어요.",
};

export function generateStaticParams() {
  return stageSchema.options.map((stage) => ({ stage }));
}

export default async function GuidePage({
  params,
}: {
  params: Promise<{ stage: string }>;
}) {
  const { stage: rawStage } = await params;
  const decodedStage = decodeURIComponent(rawStage);
  const parsed = stageSchema.safeParse(decodedStage);
  if (!parsed.success) {
    notFound();
  }
  const stage = parsed.data;
  const stagePrograms = programs.filter((p) => p.stages.includes(stage));

  return (
    <main className="mx-auto max-w-[1200px] px-6 py-12">
      <h1 className="font-display text-[32px] text-coral">{stage} 단계 지원제도</h1>
      <p className="mt-3 text-brown/80">{STAGE_INTRO[stage]}</p>

      <div className="mt-8">
        {stagePrograms.length === 0 ? (
          <p className="text-brown/60">이 단계에 등록된 지원사업이 아직 없어요.</p>
        ) : (
          <ProgramGroupedGrid programs={stagePrograms} />
        )}
      </div>
    </main>
  );
}
```

- [ ] **Step 2: 빌드 검증**

Run: `npm run build`
Expected: `Compiled successfully`, 에러 없음

- [ ] **Step 3: 브라우저로 확인**

Run: `npm run dev`가 실행 중인 상태에서 `http://localhost:3000/guide/임신준비` 접속
Expected: "정부 지원사업" 섹션과 "광역 지원사업" 섹션 제목이 보이고, 각 섹션 안에 카드가 2열로 배치됨(좁은 화면에서는 1열). 카드 패딩이 기존보다 좁아짐.

- [ ] **Step 4: 커밋**

```bash
git add app/guide/[stage]/page.tsx
git commit -m "feat: 단계별 제도 소개 페이지에 기관별 2열 그리드 적용"
```

---

### Task 5: `/programs` 페이지에 적용

**Files:**
- Modify: `app/programs/page.tsx`

**Interfaces:**
- Consumes: `ProgramGroupedGrid` from `components/programs/ProgramGroupedGrid.tsx`

- [ ] **Step 1: `app/programs/page.tsx` 전체를 아래로 교체**

```tsx
"use client";

import { useMemo, useState } from "react";
import { programs } from "@/lib/data/programs";
import { stageSchema, agencyTypeSchema, categorySchema, type Stage } from "@/lib/schemas";
import { ProgramGroupedGrid } from "@/components/programs/ProgramGroupedGrid";

const ALL = "전체";

export default function ProgramsPage() {
  const [stageFilter, setStageFilter] = useState<string>(ALL);
  const [agencyFilter, setAgencyFilter] = useState<string>(ALL);
  const [categoryFilter, setCategoryFilter] = useState<string>(ALL);

  const filtered = useMemo(() => {
    return programs.filter((p) => {
      if (stageFilter !== ALL && !p.stages.includes(stageFilter as Stage)) return false;
      if (agencyFilter !== ALL && p.agencyType !== agencyFilter) return false;
      if (categoryFilter !== ALL && p.category !== categoryFilter) return false;
      return true;
    });
  }, [stageFilter, agencyFilter, categoryFilter]);

  return (
    <main className="mx-auto max-w-[1200px] px-6 py-12">
      <h1 className="font-display text-[32px] text-coral">전체 지원사업</h1>

      <div className="mt-6 flex flex-wrap gap-4">
        <select
          value={stageFilter}
          onChange={(e) => setStageFilter(e.target.value)}
          className="rounded-button border-2 border-brown/20 px-3 py-2"
        >
          <option value={ALL}>단계 전체</option>
          {stageSchema.options.map((s) => (
            <option key={s} value={s}>
              {s}
            </option>
          ))}
        </select>
        <select
          value={agencyFilter}
          onChange={(e) => setAgencyFilter(e.target.value)}
          className="rounded-button border-2 border-brown/20 px-3 py-2"
        >
          <option value={ALL}>기관 전체</option>
          {agencyTypeSchema.options.map((a) => (
            <option key={a} value={a}>
              {a}
            </option>
          ))}
        </select>
        <select
          value={categoryFilter}
          onChange={(e) => setCategoryFilter(e.target.value)}
          className="rounded-button border-2 border-brown/20 px-3 py-2"
        >
          <option value={ALL}>카테고리 전체</option>
          {categorySchema.options.map((c) => (
            <option key={c} value={c}>
              {c}
            </option>
          ))}
        </select>
      </div>

      <div className="mt-8">
        {filtered.length === 0 ? (
          <p className="text-brown/60">
            선택한 조건에 맞는 지원사업이 아직 없어요. 다른 조건으로 다시 확인해 보세요.
          </p>
        ) : (
          <ProgramGroupedGrid programs={filtered} />
        )}
      </div>
    </main>
  );
}
```

- [ ] **Step 2: 빌드 검증**

Run: `npm run build`
Expected: `Compiled successfully`, 에러 없음

- [ ] **Step 3: 브라우저로 확인**

Run: `npm run dev`가 실행 중인 상태에서 `http://localhost:3000/programs` 접속
Expected: 필터 없이 "정부/광역/기초 지원사업" 3개 섹션이 2열 그리드로 보임. "기관" 필터를 "기초"로 바꾸면 "기초 지원사업" 섹션 하나만 남음.

- [ ] **Step 4: 커밋**

```bash
git add app/programs/page.tsx
git commit -m "feat: 전체 지원사업 페이지에 기관별 2열 그리드 적용"
```

---

### Task 6: 검색 결과(`ResultList`)에 적용

**Files:**
- Modify: `components/search/ResultList.tsx`

**Interfaces:**
- Consumes: `ProgramGroupedGrid` from `components/programs/ProgramGroupedGrid.tsx`

- [ ] **Step 1: `components/search/ResultList.tsx` 전체를 아래로 교체**

```tsx
import type { SupportProgram } from "@/lib/schemas";
import { ProgramGroupedGrid } from "@/components/programs/ProgramGroupedGrid";

export function ResultList({ programs }: { programs: SupportProgram[] }) {
  if (programs.length === 0) {
    return (
      <p className="text-brown/60">
        조건에 맞는 지원사업을 찾지 못했어요. 입력하신 조건을 다시 확인해 주세요.
      </p>
    );
  }

  return <ProgramGroupedGrid programs={programs} showEligibilityWarning />;
}
```

- [ ] **Step 2: 빌드 검증**

Run: `npm run build`
Expected: `Compiled successfully`, 에러 없음

- [ ] **Step 3: 브라우저로 확인**

Run: `npm run dev`가 실행 중인 상태에서 `http://localhost:3000/search` 접속 후 서울특별시/관악구, 출산 완료(오늘 날짜), 부모나이 32, 가구원수 3, 가구월소득 4000000으로 검색
Expected: 결과가 기관별 섹션 + 2열 그리드로 보이고, note만 있는 사업(예: 첫만남이용권)에는 "⚠️ 세부조건 확인 필요" 뱃지가 계속 표시됨

- [ ] **Step 4: 전체 테스트 스위트 및 상세 페이지 회귀 확인**

Run: `npm run test`
Expected: 모든 테스트 PASS (Task 1에서 추가된 4개 포함)

브라우저에서 `http://localhost:3000/programs/gov-parental-allowance` 접속
Expected: 상세 페이지 카드는 이번 변경 이전과 동일하게 넓은 패딩(`p-6`)으로 보임 — `Card`의 `compact` 기본값이 `false`이므로 상세 페이지는 영향받지 않아야 함

- [ ] **Step 5: 커밋**

```bash
git add components/search/ResultList.tsx
git commit -m "feat: 검색 결과에 기관별 2열 그리드 적용"
```

## Self-Review

- **스펙 커버리지**: 공통 컴포넌트 추출(Task 1~3), 여백/높이 축소(Task 2의 `compact`), 2열 그리드+기관별 섹션(Task 3), 3곳 적용(Task 4~6), `groupProgramsByAgency` 유닛 테스트(Task 1) — 스펙의 모든 항목이 태스크로 매핑됨.
- **플레이스홀더 스캔**: 없음 — 모든 코드가 완전한 형태로 작성됨.
- **타입 일관성**: `AgencyGroup`(Task 1) → `ProgramGroupedGrid`(Task 3)에서 그대로 사용. `ProgramCard`의 `showEligibilityWarning?: boolean`(Task 2)과 `ProgramGroupedGrid`의 `showEligibilityWarning?: boolean`(Task 3)이 동일한 이름·타입으로 전달됨. `Card`의 `compact?: boolean`(Task 2)이 `ProgramCard`에서 `compact`(단축 표기, `compact={true}`와 동일)로 정확히 전달됨.
